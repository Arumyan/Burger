import React from 'react';
import classes from './Toolbar.module.scss';

const Toolbar = (props) => {
  return (
    <header>
      <div>MENU</div>
      <div>LOGO</div>
      <nav>...</nav>
    </header>
  );
};

export default Toolbar;
